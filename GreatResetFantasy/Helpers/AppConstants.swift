//
//  AppConstants.swift
//  GreatResetFantasy
//

import Foundation

enum AppConstants {
    /// App display name (share cards, poll headers).
    static let appName = "Great Reset Fantasy"
    /// Avatar initials for admin/official polls.
    static let adminAvatarLabel = "GR"
    /// Title used for share link previews (simulator snapshot, poll snapshot).
    static let sharePreviewTitle = "Reset sentiment snapshot"
    /// URL scheme for deep links / sharing app.
    static let appURLScheme = "greatresetfantasy"
}
